dist/
build/

tools/stats-collector/config.json
tools/stats-collector/native/com.geminifoldersantigravity.filereader.json
tools/store-publisher/config.json
tools/store-publisher/.amo-previews-state.json

*.zip
*.tar.gz

.DS_Store
Thumbs.db
.claude/

Marketing/reddit-launch-plan.md

node_modules/

__pycache__/
*.pyc
*.pyo

.idea/
*.iml

# Hyperframes brag video — regenerable verification frames
brag-output/composition/snapshots/

# Per-agent copies of CLAUDE.md (e.g. AGENTS.md for Codex). Local-only: a second
# copy in the repo drifts silently, and a blind name substitution has already
# corrupted product facts in one (Claude the supported platform became "Codex").
# CLAUDE.md is the single source of truth — regenerate the copy, don't commit it.
AGENTS.md
